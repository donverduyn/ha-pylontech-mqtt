# Runtime requirement of the Docker sidecar (docker/main.py), needed here so
# tests can import it — see requirements_runtime.txt for the full picture,
# including the HA integration's own manifest.json requirement (paho-mqtt).
pyserial>=3.5

# Testing — bundles homeassistant core, pytest, pytest-asyncio, and all HA test fixtures
pytest-homeassistant-custom-component
# Coverage reporting; already pulled in transitively but pinned here directly
# since CI now invokes it explicitly (pytest --cov).
pytest-cov

# Code quality
ruff
pyright
mypy
# Runs the .pre-commit-config.yaml hooks; see CONTRIBUTING.md.
pre-commit
# Wheel-bundled shellcheck binary. Pinned only here, not separately in
# .pre-commit-config.yaml: that hook calls this package's console script as a
# local hook (language: system) instead of pulling its own copy via the
# shellcheck-py mirror repo, so there's one version of truth instead of two
# pins that can drift out of sync.
shellcheck-py
# Stubs for pyserial, imported by docker/main.py; mypy's strict mode requires
# an explicit stub package rather than falling back to untyped/Any.
types-pyserial

# Vulnerability scanning
pip-audit
