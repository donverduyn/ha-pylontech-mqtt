# Test-only environment pinned to the minimum Home Assistant version this
# integration declares support for (hacs.json's "homeassistant" field).
# Tooling such as Ruff, Mypy, Pyright, pre-commit, and pip-audit runs only in
# the current development environment; keeping it out of this input prevents
# unrelated tool constraints from affecting minimum-HA compatibility.
#
# The pin below and hacs.json's floor are refreshed automatically by
# `scripts/update_dependencies.py --min-ha-version-only` (see
# .github/workflows/min-ha-version-update.yaml), targeting whichever HA
# release was current MIN_HA_VERSION_MONTHS_BEHIND months ago. If that
# release's dependency set doesn't resolve from scratch (this has happened
# before: a yanked aiohttp release, an HA mqtt component still pinning
# paho-mqtt 1.x), the automation walks forward release-by-release until one
# does — never backward, since the point is dropping old support, not
# extending it. A human still reviews and merges the PR it opens; dropping
# support for an HA version is a deliberate call, not a mechanical bump.
pyserial>=3.5

# Exact version keeps this in lockstep with hacs.json's floor — see the
# comment above for how/why it moves.
pytest-homeassistant-custom-component==0.13.320
# Coverage reporting; already pulled in transitively but pinned here directly
# since CI now invokes it explicitly (pytest --cov).
pytest-cov
