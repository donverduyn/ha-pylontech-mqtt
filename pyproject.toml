[tool.pytest.ini_options]
testpaths   = ["tests"]
pythonpath  = ["tests", "."]     # "." makes custom_components importable for HA tests
addopts     = "-v --tb=short --allow-hosts=127.0.0.1,localhost"
asyncio_mode = "auto"
