name: Create or update automation PR
description: >
  Commits working-tree changes to a dedicated branch, pushes it using the
  short-lived token supplied by the caller, and creates or updates the PR.

inputs:
  branch:
    required: true
    description: "Branch to commit and push changes to"
  base-branch:
    required: true
    description: "Base branch for the pull request"
  commit-message:
    required: true
  pr-title:
    required: true
  pr-body:
    required: true
  app-slug:
    required: true
    description: "Slug of the GitHub App used as the commit author"
  gh-token:
    required: true
    description: "Short-lived token used by git and the gh CLI"

outputs:
  changed:
    description: "'true' if there were working-tree changes to commit"
    value: ${{ steps.commit.outputs.changed }}

runs:
  using: composite
  steps:
    - id: commit
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.gh-token }}
        APP_SLUG: ${{ inputs.app-slug }}
        BRANCH: ${{ inputs.branch }}
        BASE_BRANCH: ${{ inputs.base-branch }}
        COMMIT_MESSAGE: ${{ inputs.commit-message }}
        PR_TITLE: ${{ inputs.pr-title }}
        PR_BODY: ${{ inputs.pr-body }}
      # The logic lives in .github/scripts/create-or-update-pr.sh rather than
      # inline here so that meta-lint's ShellCheck step actually covers it:
      # actionlint does not lint composite actions, so shell written in this
      # file would get neither actionlint's checks nor the shellcheck pass it
      # embeds for workflow `run:` blocks. See that script's header.
      run: .github/scripts/create-or-update-pr.sh
